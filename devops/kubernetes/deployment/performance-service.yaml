apiVersion: apps/v1
kind: Deployment
metadata:
  name: performance-service
  namespace: revworkforce
spec:
  replicas: 1
  selector:
    matchLabels:
      app: performance-service
  template:
    metadata:
      labels:
        app: performance-service
    spec:
      containers:
      - name: performance-service
        image: revworkforce/performance-service:latest
        ports:
        - containerPort: 8083
        env:
        - name: MYSQL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: MYSQL_PASSWORD
        - name: SPRING_DATASOURCE_URL
          value: "$(MYSQL_URL)performance_db"
        envFrom:
        - configMapRef:
            name: app-config
---
apiVersion: v1
kind: Service
metadata:
  name: performance-service
  namespace: revworkforce
spec:
  selector:
    app: performance-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
